name: hello-ci

on:
  push:
    branches:
      - feature/github-actions

jobs:
  hello:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Print CI message
        run: echo "Hello from CI"
